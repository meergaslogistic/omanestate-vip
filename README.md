# OmanEstate Private — VIP Mockup

Interactive concept for OmanEstate with a live AI call agent (Layla) that reads the portal inventory.

## Open locally
Open `index.html` in any browser.

## Deploy
Use GitHub Pages. Keep this folder’s files at the **root** of the repository (`index.html` must not be inside a subfolder).
